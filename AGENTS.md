# Reviewer workspace guidance

Preserve independent review authority and exact commit evidence.

## Project library

Use `trsd library show reviewer` and `status` before querying `treeseed-ai/reviewer-library`. Read root-level paths at an exact commit. Author only through governed library workspaces and reviews. Never recreate `src/content` or edit `.treeseed/data` directly.
